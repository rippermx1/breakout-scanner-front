import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

import { VolumeCalculatorRoutingModule } from './volume-calculator-routing.module';
import { ReactiveFormsModule } from '@angular/forms';


@NgModule({
  declarations: [],
  imports: [
    CommonModule,
    VolumeCalculatorRoutingModule,
    ReactiveFormsModule
  ]
})
export class VolumeCalculatorModule { }
